
// Pseudocode for Flight Price Analyzer
class Flight {
    price;
    duration;
}

mergeSort(flightList, comparator) {
    // Implement MergeSort using comparator
}

comparator(f1, f2) {
    if (f1.price == f2.price)
        return f1.duration - f2.duration;
    return f1.price - f2.price;
}
